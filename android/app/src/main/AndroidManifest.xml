<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Location permissions required by the `geolocator` plugin.
         `google_maps_flutter` used to declare these for us; after the
         migration to flutter_map we have to declare them explicitly.
         FINE = GPS-grade precision (5 m). COARSE = network/WiFi
         fallback (~100 m) so the app can still resolve a position when
         the user denies precise location on Android 12+. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Required to reach the dev backend over HTTP (cleartext) on a
         LAN IP. Already implicit on older Android; explicit here so
         Android 9+ doesn't block requests to http://192.168.x.x. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Camera is used only by the Didit KYC SDK (CNIC scan + selfie
         liveness). Declared optional so the app still installs on devices
         without one — everything except identity verification works. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <application
        android:label="ride_sharing"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Disable the Impeller renderer on Android. Impeller's OpenGLES
             backend crashes the Android emulator's GPU translation layer
             (gfxstream SIGILL in glInvalidateFramebuffer) on Intel macOS.
             Falling back to Skia avoids the crash. Remove if testing on a
             device where Impeller works. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package-visibility declarations for url_launcher on Android 11+.
             Without these, canLaunchUrl() returns false for EVERY external
             intent in a release build, silently breaking Navigate (maps),
             the call buttons, and the emergency dialer. -->
        <!-- Open web links (Google Maps directions fallback). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <!-- Turn-by-turn navigation in the Google Maps app. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="google.navigation"/>
        </intent>
        <!-- Generic geo: links (any installed maps app). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
        <!-- Phone dialer with the number pre-filled (call rider / driver / 1122). -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>
</manifest>
